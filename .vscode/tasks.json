{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build website with Sphinx (make total)",
            "type": "shell",
            "command": "make total",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build local with Sphinx (make local)",
            "type": "shell",
            "command": "make local"
        }
    ]
}